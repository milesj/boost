import { Command, Name } from '../src';

describe('Command', () => {
  // @Name('test')
  // class TestCommand extends Command {}
  // it('tests', () => {
  //   const command = new TestCommand();
  // });
});
