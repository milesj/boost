import { ExtType } from './types';

export const CONFIG_FOLDER = '.config';

export const PACKAGE_FILE = 'package.json';

export const DEFAULT_EXTS: ExtType[] = ['json', 'js', 'cjs', 'mjs', 'yaml', 'yml'];
